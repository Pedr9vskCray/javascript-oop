// similar to how strings work in python, you can template your strings

let value1 = 10;
let value2 = 15;
let sum = (value1 + value2);

console.log(`Value 1: ${value1}, Value 2: ${value2}, Sum: ${sum}`);

// for templating strings this way, you use `` instead of "" or ''